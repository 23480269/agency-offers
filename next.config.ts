import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // All custom configurations have been removed to ensure Next.js uses its default Turbopack settings for development.
  // This should resolve warnings related to next.config.js and potential conflicts with Prisma initialization.
};

export default nextConfig;
